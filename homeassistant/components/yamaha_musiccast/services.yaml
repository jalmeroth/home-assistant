join:
  description: Group player together.
  fields:
    master:
      description: Entity ID of the player that should become the coordinator of the group.
      example: 'media_player.living_room_yamaha_musiccast'
    entity_id:
      description: Entity ID(s) that will join the group.
      example: 'media_player.office_yamaha_musiccast'

unjoin:
  description: Unjoin the player from a group.
  fields:
    entity_id:
      description: Entity ID(s) that will be unjoined from their group.
      example: 'media_player.office_yamaha_musiccast'
